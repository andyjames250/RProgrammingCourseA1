complete("specdata", c(2, 4, 8, 10, 12))
complete("specdata", 30:25)
complete("specdata", 3)
submit()
submit()
submit()
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
completecases <- complete(directory)
completecases2 <- completecases[nobs >= threhsold]
corr <- completecases2
corr
}
corr("specdata", 0)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
completecases <- complete(directory)
completecases2 <- completecases[nobs >= threshold]
corr <- completecases2
corr
}
corr("specdata", 0)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
completecases <- complete(directory)
completecases2 <- completecases[completecases$nobs >= threshold]
corr <- completecases2
corr
}
corr("specdata", 0)
completecases <- complete("specdata")
completecases
completecases2 <- completecases[completecases$nobs >= threshold]
completecases2 <- completecases[completecases$nobs >= 20]
x <- completecases$nobs >= 20
x
completecases2 <- completecases[x]
completecases[x]
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
corr <- completecases2
corr
}
corr("specdata")
corr("specdata",17)
z <- complete("specdata")
max(z$nobs)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- NULL
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
corr <- completecases2
corr
}
corr("specdata", 1096)
z <- corr("specdata", 1096)
z
class(z)
length(z)
nrow(z)
x <- vector()
x
length(x)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
}
corr <- id
corr
}
z <- corr("specdata", 17)
z
class(z)
z <- vector(1, 2, 3)
z <- c(1, 2, 3)
class(z)
z
1:3
class(1:3)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
}
corr <- result
corr
}
cr <- corr("specdata", 150)
cr
head(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate))
}
}
corr <- result
corr
}
cr <- corr("specdata", 150)
head(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, na.rm=TRUE))
}
}
corr <- result
corr
}
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="complete.obs"))
}
}
corr <- result
corr
}
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="complete.obs", method="pearson"))
}
}
corr <- result
corr
}
cr <- corr("specdata", 150)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
}
corr <- result
corr
}
cr <- corr("specdata", 150)
cr
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
}
corr <- result
corr
}
cr <- corr("specdata", 150)
cr
class(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
}
corr <- as.vector(result)
corr
}
cr <- corr("specdata", 150)
cr
head(cr)
cr <- corr("specdata", 2000)
cr
summary(cr)
class(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- cbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
}
#         corr <- as.vector(result)
corr <- result
corr
}
cr <- corr("specdata", 150)
cr
class(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector()
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
result <- as.vector(result)
}
corr <- result
corr
}
cr <- corr("specdata", 150)
head(cr)
cr <- corr("specdata", 2000)
cr
class(Cr)
class(cr)
x <- vector()
x
class(x)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector("numeric", 0)
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="pairwise.complete.obs", method="pearson"))
}
result <- as.vector(result)
}
corr <- result
corr
}
cr <- corr("specdata", 2000)
cr
cr <- corr("specdata", 150)
head(cr)
class(Cr)
class(cr)
cr <- corr("specdata", 2000)
class(cr)
cr <- corr("specdata", 150)
head(cr)
summary(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector("numeric", 0)
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="complete.obs", method="pearson"))
}
result <- as.vector(result)
}
corr <- result
corr
}
cr <- corr("specdata", 150)
head(cr)
summary(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector("numeric", 0)
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs >= threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="complete.obs", method="spearman"))
}
result <- as.vector(result)
}
corr <- result
corr
}
cr <- corr("specdata", 150)
head(cr)
corr <- function(directory, threshold = 0) {
## 'directory' is a character vector of length 1 indicating
## the location of the CSV files
## 'threshold' is a numeric vector of length 1 indicating the
## number of completely observed observations (on all
## variables) required to compute the correlation between
## nitrate and sulfate; the default is 0
## Return a numeric vector of correlations
result <- vector("numeric", 0)
completecases <- complete(directory)
completecases2 <- subset(completecases, nobs > threshold)
if(nrow(completecases2) > 0 ) {
id <- completecases2$id
filenames <- paste(formatC(id, width=3, format="d", flag="0"), ".csv", sep = "")
data <- NULL
for(i in 1:length(filenames)) {
data <- read.csv(paste(directory, filenames[i], sep = "/"))
result <- rbind(result, cor(data$sulfate, data$nitrate, use="complete.obs", method="pearson"))
}
result <- as.vector(result)
}
corr <- result
corr
}
cr <- corr("specdata", 150)
head(cr)
summary(cr)
cr <- corr("specdata", 400)
head(cr)
summary(cr)
cr <- corr("specdata", 5000)
summary(cr)
length(cr)
cr <- corr("specdata")
summary(cr)
length(cr)
submit()
submit()
submit()
